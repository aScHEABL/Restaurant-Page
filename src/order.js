import './style.css';
import { loadWelcomePage, loadOrderPage, loadMenuPage, loadReservationPage, loadCareerPage } from './index.js';

export default function order () {
    console.log(`This is the order page!`);
}